# Copy to .env (which git ignores) to see the Spotify section in local previews.
# The same three values go in GitHub → Settings → Secrets and variables → Actions.
# How to get them: docs/spotify-setup.md
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REFRESH_TOKEN=
